<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      A persistent WebSocket connection to choosh-relayd, plus FCM as the
      backstop when that connection isn't alive (DESIGN.md §7) is the only
      network capability Choosh has. POST_NOTIFICATIONS is required from
      API 33 to actually show an FCM-delivered notification; there is
      deliberately no foreground-service permission and no storage
      permission yet — each arrives with the milestone that actually needs
      it.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@drawable/ic_launcher"
        android:label="Choosh"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.Choosh">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".notifications.ChooshFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
</manifest>
